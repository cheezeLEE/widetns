<?xml version="1.0" encoding="UTF-8"?>

<!-- DTD 선언 -->
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	<mapper namespace="EmpMapper">
	
	<!-- 현재 시간 조회 -->
	<select id="getTime" resultType="String"> 
		SELECT now()
	</select>
	
	<!-- 데이터 삽입 -->
	<insert id = "insertEmp">
		INSERT INTO employee(kor_name, eng_name, addr1, addr2, addr3, pos, dept, career, emp_level, join_date, cert, photo, reg_writer) 
		VALUES(#{kor_name}, #{eng_name}, #{addr1}, #{addr2}, #{addr3}, #{pos}, #{dept}, #{career}, #{emp_level}, #{join_date}, #{cert}, #{photo}, #{reg_writer})
	</insert>
	
	<!--데이터 조회 -->
	<select id="selectEmp" resultType="com.widetns.projmp.dao.EmployeeVO">
		SELECT * FROM employee WHERE emp_num = #{emp_num}
	</select>
	
</mapper>